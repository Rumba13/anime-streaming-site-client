import {css, Theme} from "@emotion/react";

export const profilePopupStyles = (theme:Theme) => css`

`
export const buttonsStyles = (theme:Theme) => css`
    margin: 10px 5px 0;
    background-color: rgba(19, 19, 19, 0.9);
    border-radius: 14px;
`

